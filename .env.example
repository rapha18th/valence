# Leave empty to call PubChem directly from the browser (works today).
# Set to "/api/pug" to route PubChem calls through the bundled Vercel edge proxy
# if a specific endpoint ever rejects browser CORS.
VITE_PUBCHEM_PROXY=
